import { SafeAreaView } from "react-native";
import { Text } from "react-native";

function Modals() {
  return (
    <SafeAreaView>
      <Text>Modals</Text>
    </SafeAreaView>
  );
}

export default Modals;
